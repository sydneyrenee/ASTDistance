package fixtures

// Plain line comment (not doc).
// Another plain line comment.

/**
 * KDoc block (doc).
 *
 * Extra line for counting.
 */
class KDocExample

/* Plain block comment (NOT doc), even though it contains * characters. */
/* Another plain block comment * with stars * (NOT doc). */

    /// Triple-slash doc comment with leading whitespace (doc).
    //! Bang doc comment with leading whitespace (doc).

// End.

